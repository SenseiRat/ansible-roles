---
# Tasks to be performed on Arch systems
- name: Install base packages for Ubuntu Linux
  apt:
    name: "{{ ubuntu_packages }}"
    state: latest
    update_cache: true
    install_recommends: true